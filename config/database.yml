# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
development:
  database: jst_testing_development
  adapter: postgresql
  username: root
  password:
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  database: jst_testing_test
  adapter: postgresql
  username: root
  password:
  host: localhost

production:
  database: jst_testing
  adapter: postgresql
  username: root
  password:
  host: localhost